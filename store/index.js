const redirectPath = 'posts'

export const state = () => ({
  AfterLogin: {
    redirectPath: {
      name: redirectPath
    }
  }
})

export const getters = {}

export const mutations = {}

export const actions = {}
